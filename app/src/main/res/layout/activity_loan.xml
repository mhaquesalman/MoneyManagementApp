<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.LoanActivity">

    <TextView
        android:id="@+id/txtLoan"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:text="Loan Activity"
        android:textAppearance="@style/TextAppearance.AppCompat.Headline" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/loanRecView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/txtLoan"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="60dp"/>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavView"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:background="@drawable/background_bottom_navbar"
        app:itemTextColor="@color/bottom_navitem_color"
        app:itemIconTint="@color/bottom_navitem_color"
        app:menu="@menu/bottom_nav_menu" />

</RelativeLayout>